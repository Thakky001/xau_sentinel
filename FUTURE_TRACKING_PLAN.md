# 🎯 แผนพัฒนาระบบ Order-by-Order Tracking (Paper Trading ผ่าน Google Sheets)

นี่คือแผนฉบับสมบูรณ์ที่ผสมผสานความท้าทาย (ทุน $100) และความเสถียร (Google Sheets) เพื่อให้บอทรันบน Render ได้อย่างปลอดภัยแม้เซิร์ฟเวอร์จะรีสตาร์ท

## > [!IMPORTANT]
## User Review Required
กรุณาตรวจสอบโครงสร้างแผนนี้:
1. **เรื่อง Google Sheets:** คุณพี่ต้องเตรียมไฟล์ `credentials.json` (หรือข้อความ JSON) และ **Sheet ID** เอาไว้ใส่ใน `.env` นะครับ (ผมจะบอกวิธีตั้งค่าตอนเริ่มงาน)
2. **เรื่องทุน $100:** การตั้ง TP/SL ในระยะสั้น ถ้าทองสวิง 1 เหรียญที่ 0.01 Lot จะเท่ากับ 10 cents ($0.10) ทุน 100 เหรียญถือว่าบริหารความเสี่ยงได้พอดีครับ

ถ้าคุณพี่เห็นด้วยกับแผนรวมฉบับนี้ กด **Proceed** ได้เลยครับ!

## Proposed Changes

---

### 1. ระบบจัดการสถานะ & ฐานข้อมูล (Google Sheets Integration)
เปลี่ยนจากการเขียนไฟล์ local มาใช้คลาวด์เพื่อป้องกันข้อมูลหายเมื่อ Render รีสตาร์ท

- **การตั้งค่าพอร์ต:** ทุนเริ่มต้น **$100**, ขนาดไม้ **0.01 Lot**
- **ติดตั้ง Package:** เพิ่ม `googleapis` ใน `package.json`
#### [NEW] [sheets.js](file:///d:/git/XAU_Sentinel/lib/sheets.js)
- **`initSheet()`:** ทำงานตอนเซิร์ฟเวอร์เปิดครั้งแรก เพื่อตรวจสอบว่าตารางชีตมีหัวข้อคอลัมน์ (Headers) หรือยัง? ถ้ายังไม่มี บอทจะ "สร้างโครงสร้างตารางและหัวข้อให้อัตโนมัติ" (Auto-initialize) คุณพี่แค่โยนลิ้งก์ชีตเปล่าๆ มาก็พอครับ
- **`appendTrade(...)`:** บันทึกออเดอร์ใหม่สถานะ `PENDING`
- **`updateTradeResult(...)`:** อัปเดตสถานะเป็น `WIN` หรือ `LOSS` เมื่อรู้ผล พร้อมคำนวณกำไร/ขาดทุน
- **`getPendingTrade()`:** ดึงออเดอร์ล่าสุดที่ยัง PENDING อยู่เพื่อสานต่องาน

#### 📊 โครงสร้างคอลัมน์ใน Google Sheets (Database Schema)
ตารางชีตที่เราจะสร้าง จะเปรียบเสมือนสมุดบัญชี (Ledger) โดยแบ่งเป็นคอลัมน์ดังนี้:
*   **A | Trade ID:** รหัสออเดอร์ (เช่น `#001`)
*   **B | Date:** วัน-เวลาที่เปิดไม้
*   **C | Action:** `BUY` หรือ `SELL`
*   **D | Entry:** ราคาตอนเข้าซื้อ
*   **E | TP:** เป้ากำไร
*   **F | SL:** จุดตัดขาดทุน
*   **G | BE (Breakeven):** จุดที่ AI สั่งให้บังหน้าทุน
*   **H | Status:** สถานะปัจจุบัน (`PENDING` / `WIN` / `LOSS` / `BREAKEVEN`)
*   **I | Close Price:** ราคาตอนที่ปิดไม้ (ชน TP/SL)
*   **J | PnL:** กำไร/ขาดทุนของไม้นี้ (เช่น `+$10`, `-$10`, `$0`)
*   **K | Balance:** ยอดเงินคงเหลือในพอร์ตจำลอง (อัปเดตแบบสะสม)

---

### 2. อัปเดต Prompt และแกะข้อมูลเป้าหมายจาก AI

#### [MODIFY] [bot.js](file:///d:/git/XAU_Sentinel/lib/bot.js)
- **Prompt:** สั่งให้ AI วิเคราะห์และแนบก้อน JSON ไว้ท้ายสุด โดยต้องกำหนดจุดบังหน้าทุน (`be`) มาให้ด้วย เช่น:
  `<trade>{"action":"BUY", "entry": 2400.50, "tp": 2410.00, "sl": 2390.00, "be": 2405.00}</trade>`
- **Logic:** สกัดก้อน `<trade>` ออกมา แล้วเรียกใช้ `appendTrade()` ใน `sheets.js`
- **Telegram:** ลบแท็ก `<trade>` ออกก่อนส่งให้คุณพี่ เพื่อความสะอาดตา

---

### 3. สร้างโหมดเฝ้ากราฟ (Tracking Mode)

#### [NEW] [tracker.js](file:///d:/git/XAU_Sentinel/lib/tracker.js)
ไฟล์นี้จะทำหน้าที่เป็น "ยามเฝ้าออเดอร์" (ทำงานเมื่อมีไม้ PENDING) โดยใช้เทคโนโลยี **Real-time WebSockets**:
- **`monitorActiveTrade(trade)`:** แทนที่จะตั้งเวลาเช็คทุก 1 นาที บอทจะเปิดการเชื่อมต่อแบบ **Tick Stream (สตรีมราคาแบบวินาทีต่อวินาที)** จาก Deriv API ทันที
- **ชน TP:** ถ้าราคา Tick ปัจจุบันกระชากโดน TP ปุ๊บ บอทจะคำนวณกำไร, อัปเดตชีตเป็น `WIN`, และส่ง Telegram ทันทีแบบเสี้ยววินาที!
- **ชน SL:** ถ้าราคา Tick ชน SL, อัปเดตชีตเป็น `LOSS`, ส่ง Telegram แจ้งเตือนทันที
- **ระบบบังหน้าทุน (Breakeven):** ถ้าราคา Tick วิ่งผ่านจุด `be` ที่ AI กำหนดไว้ บอทจะอัปเดตชีตเพื่อเลื่อน SL มาบังหน้าทุน และส่ง Telegram ทันที
- **การรอจบไม้:** บอทจะเปิดสตรีมรับราคาต่อไปเรื่อยๆ จนกว่าออเดอร์จะเคลียร์จบ (พอจบแล้วจะปิดสตรีมเพื่อประหยัดเน็ต)

---

### 4. ปรับจูนระบบผู้คุมคิวงาน (Scheduler)

#### [MODIFY] [server.js](file:///d:/git/XAU_Sentinel/server.js)
วางระบบ State Machine เพื่อควบคุมโหมดการทำงาน:
- ทุกครั้งที่เซิร์ฟเวอร์เปิด จะเรียก `getPendingTrade()` จาก Google Sheets
- **ถ้ามี PENDING (Tracking Mode):** บอทจะเข้าสู่โหมดสตรีมราคา Real-time ทันที (หยุดการทำงานของลูป 15 นาทีชั่วคราวเพื่อเอาทรัพยากรมาเฝ้ากราฟ)
- **ถ้าออเดอร์จบแล้ว / หรือว่าง (Scout Mode):** กลับไปตั้งเวลาเรียกบอทหลัก (Scout) ทุกๆ **15 นาที** ตามปกติเพื่อหาจุดเข้าใหม่

## Verification Plan
1. **Test Google Sheets:** ส่งข้อมูลจำลองขึ้นชีตเพื่อยืนยันการเชื่อมต่อ API
2. **Test Tracker:** บังคับสร้างไม้ PENDING และให้ Tracker รันเช็คกราฟ M1 ว่าสามารถเคลียร์บิลเป็น WIN/LOSS ได้ถูกต้องหรือไม่
3. **Test Mode Switch:** ทดสอบการสลับลูปเวลาของ `server.js` ระหว่างลูป 15 นาที และ 1 นาที
